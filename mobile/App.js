import { registerRootComponent } from 'expo';
import App from './src/App';

// Registra o componente importado do src/App como a raiz da aplicação Expo
registerRootComponent(App);

export default App;